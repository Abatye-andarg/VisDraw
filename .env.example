# Local development only. Set both passwords to different random values.
# Use shell-compatible values (for example, openssl rand -hex 24).
DB_PORT=3307
DB_USERNAME=visualnotes
DB_PASSWORD=
MYSQL_ROOT_PASSWORD=
