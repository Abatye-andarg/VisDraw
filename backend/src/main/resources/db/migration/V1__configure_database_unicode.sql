-- Define Unicode defaults for tables created in this database.
-- No database name is hard-coded, so this also runs in isolated test databases.
ALTER DATABASE CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;
